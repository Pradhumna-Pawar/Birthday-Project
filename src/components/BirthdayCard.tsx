"use client";
// Birthday message is now integrated into Driver.tsx reveal card.
// This component is intentionally disabled.
export default function BirthdayCard({ visible: _visible }: { visible: boolean }) {
  return null;
}
